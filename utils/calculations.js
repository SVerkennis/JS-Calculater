export function add() {
        const firstNumber = Number(prompt("Enter first number, please!"));
        const secondNumber = Number(prompt("Enter second number, please!"));
        alert(firstNumber + secondNumber);
}

export function subtract() {
        const firstNumber = Number(prompt("Enter first number, please!"));
        const secondNumber = Number(prompt("Enter second number, please!"));
        alert(firstNumber - secondNumber);
}

export function multiply() {
        const firstNumber = Number(prompt("Enter first number, please!"));
        const secondNumber = Number(prompt("Enter second number, please!"));
        alert(firstNumber * secondNumber);
}

export function divide() {
        const firstNumber = Number(prompt("Enter first number, please!"));
        const secondNumber = Number(prompt("Enter second number, please!"));
        alert(firstNumber / secondNumber);
}



/*

// gehört zum auskommentierenteil in der app.js
//Addition
export function add(first, second) {
        return first + second;
}

//Subtraktion
export function sub(first, second) {
        return first - second;
}

//Multiplikation
export function multi(first, second) {
        return first * second;
}

//Division
export function divi(first, second) {
        return first / second;
}

 */